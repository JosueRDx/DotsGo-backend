# MongoDB Connection String
MONGODB_URI=mongodb://localhost:27017/dotsgo

# Server Port
PORT=5000

# Orígenes permitidos para CORS (separados por coma)
# En desarrollo: agregar tus IPs locales de red
# En producción: solo dominios públicos permitidos
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000,https://dotsgo-frontend.onrender.com

# URL del frontend (para logs y referencias)
FRONTEND_URL=http://localhost:5173

# Para producción con MongoDB Atlas:
# MONGODB_URI=mongodb+srv://<usuario>:<contraseña>@<cluster>/<database>?retryWrites=true&w=majority
